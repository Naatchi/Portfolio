export const NavbarButton = ({ array }) => {
    return (
        <>
            <a> {array[0]} </a>
        </>
    )
}

export const LandingButton = ({ array }) => {
    return (
        <>
            <a> {array[0]} </a>
        </>
    )
}